{% extends "base.html.twig" %}

{% block title %}Contact - try{% endblock %}

{% block body %}
    <div class="{{ ( "1" == "1" ) ? "card" : "bg-white shadow overflow-hidden rounded-lg" }}">
        <div class="{{ ( "1" == "1" ) ? "card-header" : "px-4 py-5 border-b border-gray-200 font-medium text-gray-900 bg-gray-50" }}">
            Contact Us
        </div>
        <div class="{{ ( "1" == "1" ) ? "card-body" : "px-4 py-5" }}">
            <p>This is a contact page template for try.</p>
            <p>You can implement a contact form here.</p>
            <div class="{{ ( "1" == "1" ) ? "mt-4" : "mt-6" }}">
                <h3 class="{{ ( "1" == "1" ) ? "h5 mb-3" : "text-lg font-medium text-gray-900 mb-3" }}">Contact Information</h3>
                <div class="{{ ( "1" == "1" ) ? "row" : "grid grid-cols-1 md:grid-cols-2 gap-4" }}">
                    <div class="{{ ( "1" == "1" ) ? "col-md-6" : "" }}">
                        <p><strong>Email:</strong> info@example.com</p>
                        <p><strong>Phone:</strong> +1 (555) 123-4567</p>
                    </div>
                    <div class="{{ ( "1" == "1" ) ? "col-md-6" : "" }}">
                        <p><strong>Address:</strong> 123 Main Street</p>
                        <p><strong>City:</strong> Anytown, ST 12345</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
